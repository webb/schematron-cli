#!/usr/bin/env bash

set -o nounset -o errexit -o pipefail

IFS=' ' read -r -d '' date newest < <( \
for inpath
do printf '%s %s\00' "$(TZ=Zulu date --reference="$inpath" +'%Y-%m-%dT%k-%M-%S,%N')" "$inpath"
done | sort --zero-terminated -r )

printf '%s' "$newest"
